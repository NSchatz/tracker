# ProGuard/R8 rules for the release build. Empty for the scaffold — release minification is
# off (see app/build.gradle.kts). Rules land here when the client gains code that needs
# keep-directives (e.g. JSON model classes for the C2 report payload).
